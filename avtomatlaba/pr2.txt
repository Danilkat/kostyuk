define
	int t;
	float s,a,v0;
	# s - перемещение/ a - ускорение / v0 - начальная скорость тела / t - время за которое произошло перемещение в секундах
begin
	#формула равномерного ускорения
read(v0);
read(a);
read(t);
s = v0 * t + (a*t*t/(a-2));
write(s);

end